# HEARTBEAT.md — 定期检查（可选）

心跳触发时快速过一遍；**全部正常**则只回复 `HEARTBEAT_OK`。

## 清单

- [ ] 工作区是否有未完成事项需提醒用户（见 `memory/` 最近记录）
- [ ] 是否有失败任务 / 长时间无响应的 AgentRuntime 进程（若可观测）
- [ ] `MEMORY.md` 是否有待用户确认的草稿变更

## 回复格式（有事时）

```text
心跳汇报：
1. …
2. …
建议：…
```

无事则：`HEARTBEAT_OK`
