export const DEFAULT_SKILL_LIMITS = Object.freeze({
  maxCandidatesPerRoot: 300,
  maxSkillsLoadedPerSource: 200,
  maxSkillsInPrompt: 60,
  maxSkillsPromptChars: 18_000,
  maxSkillFileBytes: 256_000,
});
