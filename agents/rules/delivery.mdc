---
title: delivery
xrk-inject: false
---

## 交付物放哪

| 类型 | 默认路径 | 说明 |
|------|----------|------|
| 可编辑主稿、纪要、方案 | `docs/` | 按主题分子目录，如 `docs/meetings/`、`docs/briefing/` |
| 对外附件、CSV、导出文件 | `exports/` | 群里只发摘要 + 此路径，不全文粘贴 |
| 脚本、一次性命令 | `scripts/` | 说明运行前提，不要悄悄执行 |

用户指定路径时以用户为准。新建文件用 `write`；改已有用 `search_replace`，避免无意整文件覆盖。

## 怎么让用户验收

每条办事回复尽量带**可核对检查点**：

- **文件类**：相对路径 + 文件名 + 关键内容提示（如「第 3 节表格共 12 行」）。
- **文案类**：给出可直接复制的段落，或说明在 `docs/` 哪一节。
- **数据类**：说明来源（用户粘贴 / 某 CSV / 某次查询）；改过的单元格或行数。
- **未完成**：写清「还差什么、谁提供、下一步是什么」。

复杂任务可附简短清单：

```
验收：
- [ ] 打开 docs/xxx.md，确认第二节标题
- [ ] 打开 exports/xxx.csv，核对合计行
```

## 缺依赖时怎么说

工具不可用、权限不足、环境未配置时：**诚实说明**，给用户能用的替代，不要 silent fail。

| 情况 | 怎么说 | 可交付 |
|------|--------|--------|
| 不能跑命令 | 「当前不能执行安装/转换命令」 | Markdown 草稿在 `docs/`，并写明完整版还需哪一步 |
| 不能联网 | 「当前不能访问外网」 | 基于工作区已有材料完成；列明需用户自行查证的外部项 |
| 不能生成 docx/pdf | 「当前不能导出 Word/PDF」 | 先交 `docs/*.md` 或 `exports/*.csv`，并说明用户本地如何转格式 |
| 信息不足 | 「还缺：…」 | 不提交半成品冒充终稿；可给模板或填空框架 |

降级话术示例：「Word 暂时出不了，主稿在 `docs/report.md`；您本地用 Word 打开另存即可。需要我补目录或表格再说。」
