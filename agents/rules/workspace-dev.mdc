---
description: 工作区写 Core——边界 + 场景选型 + message/MCP 骨架（细则 agent-core-dev）
alwaysApply: true
xrk-inject: false
---

# 工作区写 Core

## 边界

- **写**：仅 `core/workspace-Core/`（plugin / http / workflow / events / commonconfig / www）。
- **读**：工作区 + 项目根均可（`.cursor` / `docs` / `system-Core` / `src`…）——只读学写法；禁止写回框架。
- **勿改**系统配置：`ai-workflow.yaml`、`config/default_config/`、`data/server_bots/`（写沙箱也进不去）；挂远程 MCP 用 JS。
- 用户已说「写/加」→ **直接 write**；「继续」后禁止空转重读。
- 常见需求：先对本表；不够 read **agent-core-dev**；仍不会再读底层（按需）。

## message 骨架

`core/workspace-Core/plugin/<名>.js`

```js
export class MyCmd extends PluginBase {
  constructor() {
    super({
      name: '我的命令', dsc: '#我的命令', event: 'message', priority: 5000,
      rule: [{ reg: '^#我的命令$', fnc: 'run' }],
    });
  }
  async run() { await this.reply('ok'); }
}
```

## 远程 MCP 骨架（用户给 mcpServers JSON 时）

`core/workspace-Core/workflow/<短名>-mcp.js` —— **优先这条，勿改 yaml**

```js
import AiWorkflow from '#infrastructure/ai-workflow/ai-workflow.js';
export default class RemoteMcpMount extends AiWorkflow {
  constructor() {
    super({ name: 'example-mcp', description: '挂载远程 MCP', version: '1.0.0', priority: 5000, capabilities: ['tools'] });
  }
  async init() { await super.init(); }
}
export function getMcpServers() {
  return { '服务器名': { url: 'https://…' } }; // 或 command+args；原样映射用户 JSON
}
```

验收日志：`检测到 MCP 插件服务器`；工具前缀 `remote-mcp.<服务器名>.*`。新建 `workflow/` 需重启。

## 场景选型

| 需求 | 落点 | 模式 |
|------|------|------|
| 命令 / 关键词 | `plugin` + `message` + `rule` | 上表 |
| 仅群/私 / 主人 | `event` 或 `permission:'master'` | agent-core-dev §1 |
| 多轮 | `setContext` / `finish` | §1.6 |
| 定时 | `task` + cron | §1.7 |
| 通知/请求 | `notice…` / `request…` + **`accept()`** | §1.8 |
| 调 AI | `getWorkflow('chat').process` | §1.9 / §3.4 |
| HTTP | `http/` + `HttpResponse` | §2 |
| 自研 MCP 工具 | `workflow/` + `registerMCPTool` | §3.2 |
| 挂远程 MCP | `workflow/` + **`getMcpServers`** | §0.1 / §3.5 |
| 配置项 | `commonconfig/` | §4 |

导入：裸名 `PluginBase` / `msgSegment`；HTTP → `#utils/http-utils.js`。
