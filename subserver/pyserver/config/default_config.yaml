# 首次启动复制到 data/subserver/config.yaml

server:
  host: "0.0.0.0"
  port: 8000
  reload: false
  log_level: "info"
  stdin:
    enabled: true
    prompt: "子服> "

cors:
  origins:
    - "*"

logging:
  level: "info"
  file: "logs/app.log"
  max_bytes: 10485760
  backup_count: 5
