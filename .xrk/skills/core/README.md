# `.xrk/skills/core/` — 基础技能

默认 `customSkillRoots` 第一项。在此放置基础包（每包一个目录 + `SKILL.md`），例如：

- `agent-core` · `agent-tools` · `agent-core-dev`
- `agent-skillhub` · `agent-build-skill` · `agent-memory`

办公扩展放在上一级 `.xrk/skills/`（勿塞进 `core/`）。
