# 本地参考源码克隆（不入库）
.vendor/

# Strix 本机扫描产物（见 docs/security-strix.md）
strix_runs/

# Dependencies
node_modules/
.pnpm-store/
*.lock
pnpm-lock.yaml
package-lock.json
yarn.lock

# Logs
logs/
*.log
*.log.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Runtime data
*.pid
*.seed
*.pid.lock

# Environment variables
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
# 本地 Docker 代理（可选，勿提交）
config/docker.env
.codegraph
# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# 备份/临时（避免冗余文件进仓）
tmp/
*.bak
*.orig
*.tmp
*.temp

# Agent / 浏览器自动化工具本地产物（勿入库）
.playwright-mcp/
# .xrk 运行时本地态；产品技能种子 `.xrk/skills/` 须入库（见 agents/README · tests/framework/agent-layout）
.xrk/*
!.xrk/skills/
!.xrk/skills/**
.tmp-*
tmp_*.ps1
tmp_*.mjs
tmp_*.js

# 运行期数据：见 data/.gitignore（harness-sessions / runtime *.db* / 审计等；种子与 server_bots 可入库）

# 子服务：构建产物与本地虚拟环境（源码 apis/* 示例保留入库）
subserver/pyserver/.venv/
subserver/goserver/goserver
subserver/goserver/goserver.exe
subserver/goserver/bin/
subserver/jserver/target/
subserver/rustserver/target/
subserver/netserver/bin/
subserver/netserver/obj/
subserver/**/.vs/

# 子服 pyserver 插件：仅框架示例入库，其余为本地 clone
subserver/pyserver/apis/*/
!subserver/pyserver/apis/system/
!subserver/pyserver/apis/system/**
!subserver/pyserver/apis/media-tools/
!subserver/pyserver/apis/media-tools/**
!subserver/pyserver/apis/doc-pipeline/
!subserver/pyserver/apis/doc-pipeline/**
!subserver/pyserver/apis/web-fetch/
!subserver/pyserver/apis/web-fetch/**

# Cache directories
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
__pycache__/
*.pyc

# TypeScript
*.tsbuildinfo
*.d.ts

# Coverage
coverage/
*.lcov
.nyc_output/


# Build outputs
dist/
# /xrk：dist 入库（维护者改完建议 build 后提交；也支持用户自建）
!core/system-Core/www/xrk/dist/
!core/system-Core/www/xrk/dist/**
out/
.next/
.nuxt/

# Config (sensitive)
config/server_config/

# Temporary files
trash/

# Framework tests 已入库（pnpm test / test:fast）；勿再 ignore

# 本地运维脚本默认不入库；Redis 引导必须随仓
scripts/*
!scripts/ensure-redis.mjs
!scripts/ensure-redis.cmd
!scripts/copy-runtime-assets.mjs
*.reg

# 本地 Windows 注册表/卸载项清理备份（scripts/clean-empty-shell-uninstall.ps1）
xrk-uninstall-backup-*.reg

# 办事助手：种子 agents/ 入库；运行时 data/ai-workspace/{id}/ 见 data/.gitignore
.agents/

# 业务 Core 本地部署，主仓 framework Core：system-Core
core/*
!core/system-Core/
!core/system-Core/**