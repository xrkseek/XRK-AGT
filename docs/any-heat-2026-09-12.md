# any 热度榜复测（2026-09-12）

> **基线**：`tests/baselines/any-heat-baseline.json`（2026-09-08，`totalAny=2871`，topN=15，目标单文件降幅 ≥50%）  
> **复测**：`pnpm any-heat`（`scripts/any-heat.mjs`）  
> **关联**：`docs/coding-style.md` 类型断言 · `pnpm any-heat:check`（顶层全达标才过）

## 总览

| 指标 | 基线 | 复测 | 结论 |
|------|------|------|------|
| `totalAny`（`src/**/*.ts` 词边界 `\bany\b`） | 2871 | **346** | 总降 **87.9%** |
| 命中文件数 | 134 | 29 | — |
| 基线 top15 达 ≥50% 降幅 | — | **13 / 15** | 2 文件未降 → 次日债 |

## 基线 top15：「已降 / 未降」

| 状态 | before → after | 降幅 | 路径 |
|------|----------------|------|------|
| 未降 | 164 → 130 | 20.7% | `src/agent-runtime.ts` |
| 已降 | 122 → 0 | 100% | `src/infrastructure/plugins/loader-deal.ts` |
| 已降 | 116 → 4 | 96.6% | `src/infrastructure/commonconfig/commonconfig.ts` |
| 未降 | 112 → 112 | 0.0% | `src/utils/runtime-util.ts` |
| 已降 | 96 → 0 | 100% | `src/infrastructure/log.ts` |
| 已降 | 93 → 0 | 100% | `src/infrastructure/plugins/loader-discovery.ts` |
| 已降 | 91 → 3 | 96.7% | `src/infrastructure/crawl/playwright-session.ts` |
| 已降 | 83 → 1 | 98.8% | `src/infrastructure/ai-workflow/harness-module-loop.ts` |
| 已降 | 64 → 0 | 100% | `src/infrastructure/ai-workflow/remote-mcp.ts` |
| 已降 | 62 → 1 | 98.4% | `src/infrastructure/ai-workflow/ai-workflow.ts` |
| 已降 | 62 → 0 | 100% | `src/infrastructure/http/runtime-proxy.ts` |
| 已降 | 57 → 1 | 98.2% | `src/infrastructure/http/runtime-static.ts` |
| 已降 | 52 → 12 | 76.9% | `src/infrastructure/http/runtime-middleware.ts` |
| 已降 | 52 → 0 | 100% | `src/infrastructure/renderer/browser-renderer-base.ts` |
| 已降 | 49 → 0 | 100% | `src/infrastructure/crawl/ssrf-policy.ts` |

## 次日债（未达 ≥50% 降幅）

按残留 `any` 计数定优先级（`after≥100` → P0；`≥50` → P1；其余 P2）。

| 优先级 | 路径 | before → after | 降幅 | 备注 |
|--------|------|----------------|------|------|
| **P0** | `src/agent-runtime.ts` | 164 → 130 | 20.7% | Host 门面仍大量 `any`（代理/中间件委托）；宜按方法块收窄 `Runtime*Host` / 去掉 `as any` |
| **P0** | `src/utils/runtime-util.ts` | 112 → 112 | 0.0% | 几乎未动；大工具类，按静态方法分组替换 `any` 参数/返回值 |

**次日动作建议**：先 `runtime-util` 的 I/O 与字符串 API（调用面广），再 `agent-runtime` 的 `_createProxy` / middleware 委托；每降一批跑 `pnpm any-heat` 与 `pnpm typecheck`。

## 复测命令

```bash
pnpm any-heat           # 榜 + 已降/未降 + 次日债
pnpm any-heat:check     # 顶层 15 全 ≥50% 才 exit 0（当前预期 FAIL，直至债清）
```

*记录日：2026-09-12*
