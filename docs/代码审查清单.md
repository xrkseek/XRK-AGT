# 代码审查清单

发布前与 [文档审查清单.md](文档审查清单.md) 一并使用。自动化数据以 [框架测试指南.md](框架测试指南.md) 为准。

## 本次审查（2026-09-12）

| § | 结论 | 备注 |
|---|------|------|
| 1 自动化 | ✅ | `pnpm test:fast` → **309 pass**（Node 26.8.1） |
| 2 架构 | ✅ | 口径 **11/7/18/4/4**（已与 `SYSTEM_CORE_BASELINE` 对齐） |
| 3 安全 | ✅ | 测入口仍为清单所列；见 `security-checklist` / e2e |
| 4 配置三件套 | ✅ | 本轮无新增配置字段；规则未变 |
| 5 文档 | ✅ | 已勾选 [文档审查清单](文档审查清单.md)（数字/互链/变更同步） |
| 6 Node 26 / 写法 | ✅ | `engines.node` ≥26；www 对照 `xrk-www-compat`；无 `hot-reload-base`（`no-hot-reload` ∈ fast） |
| 7 启动冒烟 | ⏭ | 本机可选：`pnpm start` / `node dist/app.js`，打开 `/xrk/`（非 CI 硬门禁） |

缺口已当场修：框架口径 15→**18**；启动命令改为 `dist/app.js`。

## 1. 自动化

```bash
pnpm typecheck   # tsc --noEmit（Node ≥ 26 ambient：src/types/node26.d.ts）
pnpm build
pnpm test:fast   # CI 默认 gate（Node ≥ 26）
pnpm test        # 含 loaders-integration；需 bootstrap-globals
pnpm test:e2e    # 真实启动冒烟
```

- [x] 验收链一次绿灯（2026-09-12）：typecheck · build · test:fast · test · test:e2e（Node v26.8.1）
- [x] any 热度复测（2026-09-12）：总降 87.9%；top15 **13/15 已降**；未降见 [any-heat-2026-09-12.md](any-heat-2026-09-12.md) 次日债（P0：`agent-runtime` · `runtime-util`）

## 2. 架构

| 检查项 | 期望 |
|--------|------|
| 业务在 `core/<name>/` | 不写 `src/` 业务 |
| HTTP / 插件 / 工作流 | 继承对应基类 |
| Tasker | 模块顶层不 `AgentRuntime.tasker.push` |
| 渲染器 | `import` 不启浏览器；截图前 `ensureLoaded()` |
| Core www | 对照 **`xrk-www-compat`**：解包正确；无裸 `randomUUID`/`AbortSignal.timeout`；勿用保留根名 `shared`；产品页只内联 |

| 框架测试口径 | 仅 `core/system-Core/` 计入 **11/7/18/4/4** |

- [x] 口径数字与 `tests/helpers/system-core.mjs` → `SYSTEM_CORE_BASELINE` 一致

## 3. 安全

| 检查项 | 期望 |
|--------|------|
| `/api/*` | 默认鉴权；`systemAuth: false` 才公开 |
| 文件路径 | `InputValidator`（拒穿越；`isPathInside`） |
| SSRF | `assertUrlSafeForFetch` **默认拒私网**；仅 `allowPrivateNetwork` / `dangerouslyAllowPrivateNetwork` 显式 true 才放行（见 `ssrf-policy`） |
| 工具审批 | `security.approval` 默认 **false**：policy/toolScan 的 ask → 拒绝；开启后主人 `#批准`/`#拒绝`（`tool-approval`） |
| 可选外部扫描 | 发版/大改鉴权后可用 Strix（[security-strix.md](security-strix.md)）；勿把 PoC 入库 |

测：`input-validator` · `input-path-fuzz` · `security-checklist` · `tool-security` · `security-smoke-e2e`。

- [x] 安全项与文档期望一致（测文件仍存在于 `tests/framework/`）

## 4. 配置三件套

`config/default_config/*.yaml` + commonconfig schema + 消费代码。文档同步见 [文档审查清单 §C](文档审查清单.md#c-配置与-api-文档)。

- [x] 本轮无新增配置字段需三件套同步

## 5. 文档（与代码一致）

勾选 [文档审查清单.md](文档审查清单.md) 全文，至少完成：**关键数字**、**互链**、**变更同步表** 三节。

- [x] 见文档审查清单「本次审查」

## 6. Node 26 与写法

`engines.node` ≥ 26.0.0；API 见 [node-26-runtime.md](node-26-runtime.md)。全局、状态、I/O、HTTP 等见 **[coding-style.md](coding-style.md)**（发布前对照文末审查清单）。改 `core/*/www` 另见 skill **`xrk-www-compat`**（浏览器兼容层 / 内联垫片）。

- [x] `package.json` engines.node ≥26

## 7. 启动冒烟

发版次日真机步骤见 **[上线底层检查单.md](上线底层检查单.md)**（启动 dist · 探活 · harness ping · `/xrk/` · 日志无 error 风暴）。

```bash
pnpm start                 # build && node dist/app.js
node dist/app.js server 8080
```

打开 `http://127.0.0.1:<port>/xrk/`，确认截图/渲染可用。

- [ ] 本机按 [上线底层检查单](上线底层检查单.md) 勾选（发版前执行；CI 不强制）
